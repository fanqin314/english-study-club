# DESIGN — 深度解析「批注阅读手账」

## 视觉世界（Visual World）

深度解析页（移动端）从「工具感分析卡片」换新为「批注阅读手账」——纸感底色、书页正文、荧光批注色。核心心智：弱化工具感，让阅读文本成为唯一焦点，句子分析以「书页批注」形式藏于字里行间，供查漏补缺。

- **底色**：暖纸感（米白 `#F6F1E6`），暗色主题用深炭纸（`#1C1A14`）。
- **正文**：衬线书页排印（正文用 `--study-font-serif`），行高 1.9，书卷气。
- **单一批注高亮**：生词用荧光笔标记（半透明琥珀底色 + 细下划线），视觉克制不喧宾夺主。
- **短语/固定搭配**：独立线型（虚线 + 青绿底色），与生词明确区分。
- **禁用**：玻璃拟态、大面积渐变、发光描边、廉价阴影。

## Token（book-note）

| 作用 | Light | Dark |
|------|-------|------|
| `--bn-paper` 纸面 | #F6F1E6 | #1C1A14 |
| `--bn-panel-bg` 内层面板 | #FBF7EE | #242119 |
| `--bn-ink` 正文墨色 | #3B3526 | #E6DFCB |
| `--bn-muted` 弱化文字 | #8A8270 | #9A927D |
| `--bn-line` 细分隔线 | #E4DCC9 | #38332A |
| `--bn-word` 生词批注 | #F5E18C | #6E5B1E |
| `--bn-word-ink` 标记上文字 | #3A3320 | #F2EAD9 |
| `--bn-phrase` 短语批注 | #4E9E8F | #57A99A |
| `--bn-accent` 强调 | #B98A3E | #D6A95C |
| `--bn-soft` 弱提示 | #8A8270 | #9A927D |

## 组件（.bn-*）

- **句卡片 `.bn-sentence`**：纸面卡片、细边框，去掉了旧版左侧主题色粗条（规避「AI 侧边条」通感）。头部为 序号·类型 + 右侧轻量折叠标签（译文/语法/知识点，`hidden` 切换 + 淡入）。
- **正文 `.bn-body`**：衬线书页排印。
- **生词 `.esc-sw.bn-word` / 短语 `.esc-sw.bn-phrase`**：荧光 / 虚线批注，`:active` 加深 + `.is-tapped` pop 微反馈。
- **生词堆 `.bn-wordpile / .bn-wchip`**：本句重点词条胶囊，与正文双向联动（`pulseLinked` 用同一 `data-word`）。
- **查词浮层 `.bn-popover`**：固定定位贴近单词，word + 词性 + 释义 + 发音 + 收藏，点外部/滚动关闭，不打断阅读流。
- **输入折叠**：解析完成后输入卡折叠为轻量条 `.bn-input-bar`，可点开展开再编辑（内容优先）。

## 动效

- 卡片错落入场（`esc-card-in`）；面板淡入（`esc-panel-in`）；浮层淡入（`bn-fade-in`）；词条点按 pop（`esc-pop`）。全部为 transform/opacity，无布局抖动。

## 全文翻译区

沿用新世界配色（纸面、`--bn-accent` 序号/进度条/图标），保留增量渲染、进度、点击跳转逻辑。